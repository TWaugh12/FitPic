<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center_horizontal">

    <!-- TextView to display the title -->
    <TextView
        android:id="@+id/fitpic_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="fitpic"
        android:textSize="48sp"
        android:textStyle="bold"
        android:textColor="@color/black"
        android:layout_marginTop="16dp" />

    <!-- ImageView to display the selected clothing image -->
    <ImageView
        android:id="@+id/image_clothing"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_gravity="center"
        android:layout_marginTop="16dp"
        android:contentDescription="Clothing image" />

    <!-- Button to add a clothing image (camera or gallery) -->
    <Button
        android:id="@+id/button_add_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="Upload Clothes" />

    <!-- TextView for the message -->
    <TextView
        android:id="@+id/message_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select a clothing type:"
        android:textSize="16sp"
        android:layout_marginTop="16dp"
        android:layout_gravity="start" />

    <!-- Spinner for clothing types -->
    <Spinner
        android:id="@+id/spinner_clothing_type"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:entries="@array/clothing_types_array" />

    <!-- Button to save the clothing item -->
    <Button
        android:id="@+id/button_save_clothing"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="16dp"
        android:text="Save" />

</LinearLayout>
